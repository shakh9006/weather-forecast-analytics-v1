# Weather Forecast Analytics Dependencies
# Core data science packages
pandas
numpy
requests
scikit-learn
matplotlib
seaborn
plotly
python-dotenv

# Database connectivity
# Note: psycopg2-binary is already included in base Airflow image
# sqlalchemy is already included in base Airflow image

# Airflow providers (may already be included)
# apache-airflow-providers-postgres
# apache-airflow-providers-http
